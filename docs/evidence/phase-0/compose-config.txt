Command:
docker compose -f compose.yaml -f compose.dev.yaml config

Result:
Passed. Docker Compose rendered backend, frontend, db, default network, and postgres_data volume configuration successfully.

Relevant services:
- backend: FastAPI service on port 8000 with dev reload command
- frontend: Vite service on port 5173 with dev command
- db: PostgreSQL 16 service on port 5432
