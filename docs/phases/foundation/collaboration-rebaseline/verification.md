# Collaboration Rebaseline Foundation Verification

## Verified Commit
`3e18950c9d6d00b50967b99ceb88178c0c74c450` was the starting `main` commit.

Verified branch commits:

- `a8cb8b7` docs: rebaseline UFC development phases
- `9e346df` docs: align data analysis and rule engine contracts
- `7580445` chore: add make based local development workflow
- `d89e260` docs: record collaboration verification results
- `3d1243a` chore: add ci verification workflow
- Current PR head: See PR #7 until merge.

## Verified At
2026-07-30 02:21:17 KST

## Environment
macOS, Docker Compose, local `.env` generated by `make init`.

## Commands
- `git switch main`
- `git pull --ff-only origin main`
- `git switch -c chore/phase-rebaseline-collaboration-foundation`
- `make init`
- `make dev`
- `make ps`
- `make migrate`
- `make test`
- `make lint`
- `make verify`
- `curl -fsS http://localhost:8000/health`
- `curl -fsS http://localhost:8000/ready`
- `curl -fsS http://localhost:5173`
- Markdown local link check over `README.md`, `AGENTS.md`, and `docs/**/*.md`

## Results
- `make init`: passed. `.env` was created from `.env.example` when missing, required local secrets were generated, and rerunning did not overwrite existing values.
- `make dev`: passed after resolving the CORS environment parsing issue documented in `docs/troubleshooting/pydantic-settings-list-env-cors.md`.
- `make ps`: passed. `db`, `backend`, and `frontend` were healthy.
- `make migrate`: passed. Alembic reported PostgreSQL transactional DDL and no pending failure.
- `make test`: passed. 39 backend tests passed with 1 Starlette/httpx deprecation warning.
- `make lint`: passed. Backend Ruff and frontend ESLint completed successfully.
- `make verify`: passed. Compose config, migration, backend tests, backend lint, frontend lint, frontend build, and `git diff --check` completed successfully.
- Markdown local link check: passed. 121 Markdown files checked with no missing local links.

## API Evidence
- `GET /health`: `{"status":"ok"}`
- `GET /ready`: `{"status":"ready"}`
- `GET http://localhost:5173`: returned the Vite HTML entrypoint.

## DB Evidence
- PostgreSQL container healthcheck passed.
- `make migrate` completed successfully against the Docker Compose PostgreSQL service.
- No new migration was added in this rebaseline task.

## Security Evidence
- `.env` is not tracked by Git.
- `.env.example` contains empty secret placeholders only.
- `make init` generates local-only values for `AUTH_TOKEN_SECRET`, `FIELD_ENCRYPTION_KEY`, `FIELD_LOOKUP_HMAC_KEY`, and `FIELD_KEY_VERSION` without overwriting existing values.
- Qwen3 prompt input restrictions were documented to exclude user identities, full transaction arrays, raw transaction memo text, tokens, ciphertext, and secrets.

## Known Limitations
- `make reset` was not executed because it intentionally deletes the PostgreSQL volume and requires `CONFIRM=1`.
- PR #6 was not merged into `main` at the time of this branch. Its behavior metrics code was not imported or duplicated.
- Qwen3/Ollama commands are profile scaffolding only. AI runtime/provider implementation remains out of scope for this task.
- Nginx remains deferred until an integration/delivery phase because no production-ready reverse proxy configuration is part of the current implementation.
